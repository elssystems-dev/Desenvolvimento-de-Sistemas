function Header() {
    return (
        <header className = "app-header">
            <h1>Lista de Tarefas</h1>
            <p>Gerenciador de lista de tarefas</p>
        </header>
    );
}

export default Header; // Permite reutilizar o componente em outros lugares (Chamando apenas o arquivo)